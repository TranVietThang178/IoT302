#include <stdio.h>

int main(void)
{
	int a, b, c, max;

	printf("a = ");
	scanf("%d", &a);
	printf("a = ");
	scanf("%d", &b);
	printf("a = ");
	scanf("%d", &c);

	max = (num1 > num2) ? ((num1 > num3) ? num1 : num3) : ((num2 > num3) ? num2 : num3);

	getchar();
	getchar();

	return 0;
}
