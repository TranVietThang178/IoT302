#include <stdio.h>

int main(void)
{
	int a, b, c, max;

	printf("a = ");
	scanf("%d", &a);
	printf("a = ");
	scanf("%d", &b);
	printf("a = ");
	scanf("%d", &c);

	max = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);

	getchar();
	getchar();

	return 0;
}
